# Database (default works with docker-compose db service)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/template

# Optional modules: auth, basket, messaging (comma-separated)
ENABLED_MODULES=auth

# Auth (when auth module enabled)
SECRET_KEY=change-me-in-production
